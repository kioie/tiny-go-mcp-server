# Binaries (root only — do not ignore cmd/tiny-go-mcp source directory)
/tiny-go-mcp
/tiny-go-mcp.exe
/tinymcp.exe
/mcp-server
mcp-server.exe
*.exe
*.test
*.prof
coverage.out
dist/
node_modules/

# OS
.DS_Store

# Smithery MCPB build artifacts
.smithery/
server.mcpb

# gemini-cli (generated at runtime in CI; keep .gemini/commands/ in git)
.gemini/settings.json
.gemini/telemetry.log
.gemini/context.json

# GitHub App credentials (optional Gemini dispatch auth)
gha-creds-*.json
